{
  "dependencies": {
    "scss": "^0.2.4"
  },
  "scripts": {
    "scss": "npx sass --no-source-map --watch scss:css",
    "scss compress": "npx sass --no-source-map scss:css --style compressed",
    "scss all": "npx sass --no-source-map scss:css",
    "scss compress all": "npx sass --no-source-map scss:css  --style compressed"
  }
}